자바에서는 메서드

자바스크립트 에서는 함수

함수란?
입력 -> 실행 -> 출력
이름이 있는 실행 단위 

함수 생성
function 함수명(변수1){
    재활용 하고 싶은 코드 작성
    변수1
}

함수호출(사용함)
함수명(변수1);

정해진 매개변수대로 안줘도 자바스크립트는 실행되니까 
조심해야함

ES6새로운 자바스크립트에서는
아무거도 안쓰면 기본값이 리터럴값이 된다
function 함수명(변수명='리터럴'){
}

이전버전의 경우는 ||을 사용하여 기본값 세팅을함

리턴밑에 코드는 실행이 안된다 

반환값: 함수 호출 이후에 함수 밖으로 전달되는값
add(5, 7);



ES6
배열을 매개변수 줄경우 
...을 붙이면 
알아서 배열로 묶어서 처리함
배열 두개를 전달할 경우 사용할 수 없다

함수의 리턴값은 하나지만
객체나 배열에 담에서 리턴할 경우 여러가지를 전달 할 수 있다 

변수를 두번 이상 쓰면 변수선언해서 쓰는게 좋고 
그게 아닌경우 변수 선언하지 말고 바로 쓴다
