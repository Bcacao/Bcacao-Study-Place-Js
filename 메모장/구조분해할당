구조분해 할당이란?

객체나 배열을 기본데이터로 해체하는것

한곳에 모아서 관리하다가 
다시 한곳에 각각 관리하고 싶다 

배열은 순서대로 
객체는 키값만 쓰면되고 원하는 키만 써도됨

객체는 키명에 :별칭 을 넣어줘서 키값을 바꿀수 있다
객체도 ...으로 나머지만 다시 객체로 담을수 있다

디스트럭처링은 사본을 복사흔거라 
변경해도 원본에 적용되지 않는다

forEach함수에서 
(변수명, idx)라고 하면 인덱스도 뽑아줌


.style이 가능한 이유는
태그 타입이기 떄문이다 

